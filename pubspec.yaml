name: el_tooltip
description: ElTooltip - a smart positioned tooltip.
version: 1.0.2
repository: https://github.com/marcelogil/el_tooltip

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: